<h1>Listing Users</h1>
<%= @errors %>
<table>
  <tr>
    <th>Username</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>


<% @users.each do |user| %>
  <tr>
    <td><%= user %></td>
    <td><%= link_to 'Reset', edit_user_path(user)%></td>
    <td><%= link_to 'Destroy', user_path(user), :confirm => 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

